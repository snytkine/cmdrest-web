You are a very experienced senior web developer specialing in modern web design. You are an expert in react.js and typescript. 

Please help me create a modern web application that uses react.js, three.js and utilized modern HTML 5 features. Website must render well in browser as well as in mobile devices.

The goal of the website is to promote this new project. The project is called "CmdRest" which is a command line tool for testing rest apis.

Design should use light background color, lots of white space. Some 3D anymation using three.js will be used to showcase the project.

The backgound will have some images that showcase technology, testing, rest api, application architecture diagrams, CI/CD diagram images etc. 

Create a new React.js project using Create React App. Must use typescript with strong types. Website must be easy to maintain and to update the content.


List some key features:
1. Home page with a hero section
2. Features including
    - Zero Code api tests creations. Test suite is created using only .yaml files. 
    - Code is compiled to native application for MacOS, Windows or Linux. No Java is requiored to be installed. Extremely fast startup and execution of tests.
    - payload bodies and schemas can be stored as external files or inline.
    - Option to create debug logs during test execution.
    - Interactive Terminal UI with colors, animations and interacive terminal menus.
    - Non-interactive mode suitable for running tests in CI/CD
    - Over 50 assertion types
    - Lifecycle callback hooks
    - Generating vibrant self-contained test reports in HTML format.
    - Ability to exclude some tests from running
    - Ability to run tests with matching tag
    - Ability to run single test from test suite.
    - Ability to just make http requests and disaplay responses without running any assertions.
    - Support for various authentication types.
    - Pre-Request scripts.
    - Use template stype placeholders in test suite to make tests more flefible
    - Support for external .env files to store parameters or secrets.

3. A lint to project's release page: https://github.com/snytkine/api-tester-cli/releases  Mention that builds are available for MacOS, Windows and Linux.

4. Link to source code with mention that this is an open source project. https://github.com/snytkine/api-tester-cli

5. Link to documentation page: https://snytkine.github.io/api-tester-cli/test-suite-configuration/


All the Code must be well documented, including commets for all classes and method and components. Eveery significate function of condition should have a comment with explanation what it's doing.

Codebase must have unit tests with maximum coverage possible. Unit tests must also include documentation and detailed comments.

The code must also utilize some sort of logging framework to that website usage and interactions can be logged to a console with the option to direct logging to external logging backend like local storage and indexDB in web browser. Please use a logging library like winston or other library that is well known, and well documented and is suitable for react.js applications.



Design should be bold, and modern. Use a color scheme that reflects technology.

Before starting a design interview me on some key decisions.


